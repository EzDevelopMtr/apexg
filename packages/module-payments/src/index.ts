/**
 * `@apexg/module-payments` — payments and instalments (RF-17 to RF-20).
 *
 * The app supplies routing and the repositories; this package owns the screens
 * and the state around them.
 */

export { default as PaymentsPage } from "./components/payments-page";
export type { PaymentsPageProps } from "./components/payments-page";
