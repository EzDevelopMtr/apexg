/**
 * Icon names used across the app.
 *
 * `core` knows nothing about React, so icons travel as names and `@apexg/ui`
 * resolves each one to a real component.
 */
export type IconName =
  | "users"
  | "card"
  | "wallet"
  | "package"
  | "chart"
  | "dumbbell"
  | "list"
  | "userPlus"
  | "userCheck"
  | "clock"
  | "userX"
  | "receipt"
  | "notebook";
