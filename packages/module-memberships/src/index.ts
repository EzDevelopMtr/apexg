/**
 * `@apexg/module-memberships` — the membership catalogue (RF-12, RF-13).
 *
 * The app supplies routing, the role and the repositories; this package owns
 * the screens and the state around them.
 */

export { default as MembershipsPage } from "./components/memberships-page";
export type { MembershipsPageProps } from "./components/memberships-page";
