{
  "name": "apexg",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo del sistema de gestion APEX GYM",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "oxlint": "^1.75.0",
    "turbo": "^2.10.11",
    "typescript": "~6.0.3"
  }
}
